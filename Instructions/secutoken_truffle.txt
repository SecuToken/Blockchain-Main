======Background Info======
accounts[0] = 0x3... (SecuToken admin address)
accounts[1] = 0x1... (Usertest address)

======Console Commands=====
truffle console --network development

// Get the deployed contract instance
let instance = await SecuToken.deployed()

// Get the contract owner
let owner = await instance.owner()

// Get the contract balance
let balance = await instance.getBalance()

// Check initial contract balance
balance.toString()

// Send Ether to the contract
await web3.eth.sendTransaction({from: accounts[0], to: instance.address, value: web3.utils.toWei("1", "ether")})

// Withdraw Ether
await instance.withdraw(web3.utils.toWei("0.5", "ether"))

// Confirm Updated balance
balance = await instance.getBalance()
balance.toString()


======Debug Commands======
console.log(owner)
console.log(instance.address)
console.log(web3.utils.fromWei(balance, "ether"))